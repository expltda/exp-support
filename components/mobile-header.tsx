import { WifiIcon, BatteryIcon, SignalIcon } from "@/components/icons"

export function MobileHeader() {
  const currentTime = new Date().toLocaleTimeString("pt-BR", {
    hour: "2-digit",
    minute: "2-digit",
  })

  return (
    <></>
  )
}
